#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BINARY="$ROOT_DIR/.local/bin/pulse-review"
cd "$ROOT_DIR"

[[ -x "$BINARY" ]] || { printf 'Приложение не собрано. Сначала запустите: bin/setup\n' >&2; exit 1; }
exec "$BINARY"
